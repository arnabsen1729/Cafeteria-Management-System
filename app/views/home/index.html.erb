<div class="jumbotron jumbotron-fluid bg-dark">

  <div class="jumbotron-background">
    <img src="<%= asset_path("background.jpg") %>" class="blur ">
  </div>

  <div class="container text-white">

    <h1 class="display-4">Welcome !!</h1>
    <p class="lead">The Cafeteria Management System</p>
    <hr class="my-4">
    <p>Get your coffee to start you day with energy</p>
    <a class="btn btn-primary btn-lg" href="/users/new" role="button">Sign-Up</a>
    <p class="mt-4 ">Already an user? <a href="/signin">Sign-In</a></p>
  </div>
</div>

<style>
.jumbotron {
  position:relative;
  overflow:hidden;
  padding-top:150px;
  height: 100vh;
}

.jumbotron .container {
  position:relative;
  z-index:2;

  background:rgba(0,0,0,0.2);
  padding:2rem;
  border:1px solid rgba(0,0,0,0.1);
  border-radius:3px;
}

.jumbotron-background {
  object-fit:cover;
  position:absolute;
  top:0;
  z-index:1;
  width:100%;
  height:100vh;
  opacity:0.5;
}

img.blur {
	-webkit-filter: blur(4px);
	filter: blur(4px);
  filter:progid:DXImageTransform.Microsoft.Blur(PixelRadius='4');
}
</style>
